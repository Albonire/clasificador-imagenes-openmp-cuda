# Dataset

Muestra representativa del dataset (subconjunto, no el total).

## Estructura
```
dataset/
  raw/          # imágenes crudas capturadas con webcam
    clase_0/
    clase_1/
  procesado/    # salida de la Etapa 1 (64×64, escala de grises)
```

> Solo subir un subconjunto representativo (~20 imágenes por clase). No subir el dataset completo.
