# Versión GPU (CUDA)

Misma red, mismos datos, entrenada con kernels CUDA propios (`train_gpu.cu`). El notebook `model-gpu.ipynb` solo compila, orquesta y mide; toda la lógica de entrenamiento vive en el `.cu`.
